At the beginning of every AI session, read the root CLAUDE.md and any applicable package-level CLAUDE.md before taking actions.

Do not modify third-party, vendor, generated dependency, or external module code unless the user explicitly asks for that. Code under this repository's `packages/` directory is Nimbalyst-owned distribution code and may be modified when the task requires it.
